import React from 'react';
import {Text, View} from 'react-native';
import {basic} from '../styles/common';

const ProfileScreen = () => {
  return (
    <View style={basic.container}>
      <Text style={basic.text}>Profile Screen</Text>
    </View>
  );
};

export default ProfileScreen;
